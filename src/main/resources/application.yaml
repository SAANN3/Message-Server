ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080

jwt:
  issuer: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
  secret: "HUI"
postgres:
  url: "$DB_URL:jdbc:postgresql://localhost:5432/server-message"